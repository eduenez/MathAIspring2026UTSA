numpy
matplotlib
scikit-learn
ipywidgets
scipy

# JAX — install the variant that matches your hardware (pick exactly one):
#
#   CPU only (any platform, safest default):
#       pip install jax
#
#   NVIDIA GPU on Linux:
#       pip install "jax[cuda12]"
#
#   Apple Silicon (macOS) — CPU backend with optional Metal acceleration;
#   note that jax-metal lacks some ops (e.g. MultiHeadAttention uses CPU fallback):
#       pip install jax jax-metal
#
# Google Colab: JAX is pre-installed; the notebook setup cell auto-configures it.
jax

keras
# keras-cv
# keras-hub

# PyTorch — used as an alternative Keras backend (e.g. to access Apple Metal GPU on macOS).
# MPS (Metal Performance Shaders) support is included in standard PyTorch builds for macOS.
torch
jupyter
mystmd
